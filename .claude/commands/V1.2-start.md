현재 단계를 v1.2로 전환하고 AI 추천 기능 작업을 순서대로 진행해줘.

## 전환 작업 (먼저 실행)

1. `CLAUDE.md` 에서 "현재 단계" 를 `v1.2` 로 업데이트
2. `.claude/agents/ai-recommender.md` 에서 Gemini 모델과 호출 방식 확정 여부 확인
   - 미정이면 중단하고 사용자에게 확정 요청
   - 확정됐으면 해당 파일 업데이트 후 진행

## v1.2 작업 목록 (순서대로)

- [ ] `pnpm add @google/generative-ai` 설치
- [ ] `frontend/domain/entities/recommendation.ts` 생성 (RecommendRequest, RecommendResult 타입)
- [ ] `frontend/domain/use-cases/recommend.ts` 생성 (buildRecommendPrompt, parseRecommendResponse)
- [ ] `frontend/infrastructure/ai/gemini-client.ts` 생성
- [ ] `frontend/app/api/recommend/route.ts` 생성
- [ ] `frontend/app/components/RecommendForm.tsx` 생성
- [ ] `frontend/app/components/RecommendResult.tsx` 생성
- [ ] 추천 결과 → 장바구니 담기 연동
- [ ] `@agent-reviewer` 리뷰
- [ ] `docs/progress.md` v1.2 완료로 업데이트

## 실행 규칙

docs/tasks.md 의 v1.1 작업이 모두 [x] 완료 상태인지 먼저 확인.
미완료 항목이 있으면 중단하고 알려줘.
완료 확인 후 위 목록을 순서대로 진행해줘.
