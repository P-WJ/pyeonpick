현재 단계를 v1.2로 전환하고 AI 추천 백엔드를 구현해줘.

## 현재 상태 확인 (먼저 실행)

1. `GEMINI_API_KEY` 환경변수가 `.env.local`에 있는지 확인
2. Gemini API quota 사용 가능 여부 확인
3. `frontend/app/api/ai/recommend/route.ts` 파일 존재 여부 확인

quota 문제가 있으면 중단하고 사용자에게 알려줘.

## 이미 완료된 항목 (건드리지 않음)

- [x] `frontend/domain/entities/recommendation.ts` — 타입 정의 완성
- [x] `frontend/domain/use-cases/recommend.ts` — 프롬프트·파싱 로직 완성
- [x] `frontend/infrastructure/gemini.ts` — Gemini API 래퍼 완성
- [x] `frontend/app/components/AiBanner.tsx` — UI 완성
- [x] `frontend/app/components/AiRecommendModal.tsx` — UI 완성

## 구현 필요 항목 (순서대로)

- [ ] `frontend/app/api/ai/recommend/route.ts` 구현
  - POST 메서드
  - body: `{ budget: number, stores?: string[] }`
  - `getProducts()` → `buildRecommendPrompt()` → `generateRecommendation()` → `parseRecommendResponse()`
  - 응답: `{ data: { combinations: [] }, error: null }`
  - budget 유효성 검증 (1000원 이상)
  - 오류 시 한국어 메시지 반환
- [ ] `pnpm tsc --noEmit` 타입 체크
- [ ] `NEXT_PUBLIC_ENABLE_AI_RECOMMEND=true` 설정 후 동작 확인
- [ ] `@agent-reviewer` 리뷰
- [ ] `docs/progress.md` v1.2 완료로 업데이트
- [ ] `CLAUDE.md` 현재 단계 v1.2로 업데이트

## 실행 규칙

- `frontend/infrastructure/gemini.ts` 의 `generateRecommendation()` 함수를 그대로 사용
- API Route는 얇게 — 비즈니스 로직은 domain use-case에만
- 같은 조건 5분 내 재요청은 Map으로 메모리 캐싱 (비용 절감)
- `MAX_PRODUCTS_IN_CONTEXT = 100` 상수 유지
