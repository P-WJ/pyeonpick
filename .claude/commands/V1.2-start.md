⚠️ v1.2는 이미 완료된 단계입니다.

## v1.2 완료 항목 (참고용)

- [x] `frontend/domain/entities/recommendation.ts` — 타입 정의
- [x] `frontend/domain/use-cases/recommend.ts` — 프롬프트 빌드 + 파싱 로직
- [x] `frontend/infrastructure/llm.ts` — Groq API 래퍼 (구 `gemini.ts`)
- [x] `frontend/app/api/ai/recommend/route.ts` — POST 핸들러 (5분 캐시 포함)
- [x] `frontend/app/components/AiBanner.tsx` — UI 완성
- [x] `frontend/app/components/AiRecommendModal.tsx` — UI 완성

## 참고

- Gemini → **Groq** 로 교체 (quota 문제로). 최종 모델은 `llama-3.1-8b-instant` (70b는 TPD 1,000이라 1회 요청도 불가)
- `GROQ_API_KEY` 환경변수 사용 (카테고리 분류와 공용)
- AI 배너 활성화: Vercel 환경변수에 `NEXT_PUBLIC_ENABLE_AI_RECOMMEND=true` 추가
