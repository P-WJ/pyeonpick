⚠️ v1.1은 이미 완료된 단계입니다 (2026-06-01).

완료된 항목은 `docs/progress.md` 에서 확인하세요.

---

## v1.1 완료 항목 (참고용)

- [x] `crawler/domain/entities.py` 에 Subscription dataclass 추가
- [x] `crawler/infrastructure/repository.py` 에 구독 관련 쿼리 추가
- [x] `crawler/use_cases/notify.py` 생성 (키워드 매칭 + 발송)
- [x] Supabase `subscriptions`, `notifications_sent` 테이블 마이그레이션 SQL
- [x] `frontend/infrastructure/repositories/subscription-repository.ts` 생성
- [x] `frontend/app/api/subscriptions/route.ts` 생성 (POST, GET, DELETE)
- [x] `frontend/app/components/SubscribeForm.tsx` 생성 (3단계 폼)
- [x] `frontend/app/notifications/page.tsx` 생성 (구독 조회/해제)
- [x] 카카오 로그인 (NextAuth v5)
- [x] 커뮤니티 게시판 (목록, 글쓰기, 댓글)

## 알림 전환 예정

현재 알림 발송은 **Resend 이메일 임시 운영** 중.
→ **카카오 알림톡으로 전환 예정 (v1.3)**
관련 작업은 `@agent-notifier` 에게 위임.
