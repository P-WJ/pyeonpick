현재 단계를 v1.1로 전환하고 알림 기능 작업을 순서대로 진행해줘.

## 전환 작업 (먼저 실행)

1. `CLAUDE.md` 에서 "현재 단계" 를 `v1.1` 로 업데이트
2. `docs/tasks.md` 에 아래 v1.1 작업 목록 추가

## v1.1 작업 목록 (순서대로)

- [ ] `crawler/domain/entities.py` 에 Subscription dataclass 추가
- [ ] `crawler/infrastructure/repository.py` 에 구독 관련 쿼리 추가
- [ ] `crawler/infrastructure/email/resend_client.py` 생성
- [ ] `crawler/use_cases/notify.py` 생성 (키워드 매칭 + 발송)
- [ ] Supabase `subscriptions`, `notifications_sent` 테이블 마이그레이션 SQL
- [ ] `frontend/infrastructure/repositories/subscription-repository.ts` 생성
- [ ] `frontend/app/api/subscriptions/route.ts` 생성 (POST, DELETE)
- [ ] `frontend/app/components/SubscribeForm.tsx` 생성
- [ ] `@agent-reviewer` 리뷰
- [ ] `docs/progress.md` v1.1 완료로 업데이트

## 실행 규칙

docs/tasks.md 의 MVP 작업이 모두 [x] 완료 상태인지 먼저 확인.
미완료 항목이 있으면 중단하고 알려줘.
완료 확인 후 위 목록을 순서대로 진행해줘.
